{
  "name": "@caliber/steward-client",
  "version": "0.1.0",
  "private": true,
  "description": "The 3-line Steward client. createSteward({baseUrl, apiKey}).pay(url, init) — POSTs the payment intent to Steward, which authorizes, signs, pays, and ledgers. Zero deps.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@caliber/steward-core": "workspace:*",
    "viem": "^2.27.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "typescript": "^5.8.3"
  }
}
