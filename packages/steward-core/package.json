{
  "name": "@caliber/steward-core",
  "version": "0.1.0",
  "private": true,
  "description": "Steward pure logic — decision types, detectors (redirect, …), incident kinds. No Express, no DB. The deterministic core every Steward surface shares (Steward Phase 1, task 10).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "policy-check": "tsx scripts/policy-check.ts",
    "llm-check": "tsx --env-file=../../.env scripts/llm-check.ts",
    "eip712-check": "tsx scripts/eip712-check.ts"
  },
  "dependencies": {
    "@caliber/x402-client": "workspace:*",
    "viem": "^2.27.3",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
