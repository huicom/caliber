{
  "name": "@caliber/hirebot",
  "version": "0.1.0",
  "private": true,
  "description": "Budget-aware demo agent that pays Caliber Metered per attestation and decides whether a trust check is worth it (Lepton Phase 1 A4).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "run-once": "tsx --env-file=../../.env src/index.ts",
    "loop": "tsx --env-file=../../.env src/index.ts --loop"
  },
  "dependencies": {
    "@arc-agents/db": "workspace:*",
    "@caliber/x402-client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
